{
  "name": "shopfluence-monorepo",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "install:all": "npm install --prefix aura-commerce && npm install --prefix backend/shopfluence-api",
    "dev:frontend": "npm run dev --prefix aura-commerce",
    "dev:backend": "npm run start:dev --prefix backend/shopfluence-api",
    "dev": "npx concurrently -n \"frontend,backend\" -c \"magenta,blue\" \"npm run dev:frontend\" \"npm run dev:backend\"",
    "build:all": "npm run build --prefix aura-commerce && npm run build --prefix backend/shopfluence-api"
  },
  "dependencies": {
    "concurrently": "^9.1.2"
  }
}
